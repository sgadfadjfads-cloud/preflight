interface:
  display_name: "Preflight"
  short_description: "Frame goals, risks, boundaries, and success before agent action"
  default_prompt: "Use Preflight when a task may hide important goals, constraints, risks, or authorization boundaries."
